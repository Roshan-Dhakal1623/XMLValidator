@model XMLValidator.Models.XmlValidationViewModel
@{
    ViewData["Title"] = "XML Validator";
}

<div class="container mt-5 xml-validator-page">
    <h1 class="text-center mb-4 xml-page-title">XML Validator</h1>

    <form asp-action="Upload" asp-controller="Home" method="post" enctype="multipart/form-data" class="border p-4 rounded shadow-sm xml-card">

        <div class="mb-3">
            <label class="form-label xml-form-label">Schema File (.xsd)</label>
            <input type="file" name="SchemaFile" class="form-control" required />
        </div>

        <div class="mb-3">
            <label class="form-label xml-form-label">XML File (.xml)</label>
            <input type="file" name="XmlFile" class="form-control" required />
        </div>

        <div class="text-center">
            <button type="submit" class="btn btn-primary px-4 xml-submit">Upload</button>
        </div>
    </form>

    @if (Model?.IsValid != null)
    {
        <div class="mt-4 xml-card">
            @if (Model.IsValid == true)
            {
                <div class="alert alert-success">
                    ✅ XML file is <strong>VALID</strong> according to the schema.
                </div>
            }
            else
            {
                <div class="alert alert-danger">
                    XML file is <strong>INVALID</strong>.
                    <ul>
                        @foreach (var error in Model.Errors)
                        {
                            <li>@error.Message</li>
                        }
                    </ul>
                </div>
            }
        </div>
    }
</div>
